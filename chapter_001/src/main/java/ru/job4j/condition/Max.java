package ru.job4j.condition;

public class Max {
    /**
     * Method for determine maximum of two digit
     * @param left value
     * @param right value
     * @return int max
     */
    public int max(int left, int right) {
        return left > right ? left : right;
    }
}